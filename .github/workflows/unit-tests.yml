name: Unit Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore DevHobby.sln

      - name: Build
        run: dotnet build DevHobby.sln --no-restore --configuration Release

      - name: Run Unit Tests Only
        # Run only unit tests, skip integration tests that require Docker
        run: dotnet test RPG.UnitTest --no-build --configuration Release --no-restore --verbosity normal
